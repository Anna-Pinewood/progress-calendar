# .env (Project Root)

# PostgreSQL Initialization Settings
# These are used by the 'db' service in docker-compose.yml to initialize the PostgreSQL container
# when it's created for the first time (or when the volume is empty).
DB_INIT_USER=postgres
DB_INIT_PASSWORD=changeme
DB_INIT_NAME=progress_calendar_db

# Port mapping for PostgreSQL
# This defines which port on your host machine will connect to port 5432 inside the db container.
# Make sure this port is free on your host.
POSTGRES_PORT_ON_HOST=5433